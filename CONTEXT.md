# MyUrls

MyUrls 管理短链接与长链接之间的映射。

## 领域术语

**长链接**：短链接指向的目标 URL。

**短键**：标识一条短链接映射的字符串，可由用户指定或由系统自动生成。短键是短链接中的路径部分，不包含协议和域名。

**短链接**：包含协议、域名和短键的完整 URL。

**短链接创建**：为长链接建立一条带有效期的短键映射。创建不会覆盖仍然有效的已有映射。

**短键冲突**：创建时，待用短键已经被一条有效映射占用。

**有效期**：短链接映射从创建到过期之间的时长。
